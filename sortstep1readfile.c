#include<stdio.h>
#include<malloc.h>
int main()
{
    int s;
    fprintf(stderr,
    "Firestone's law of forecasting\n"
    "PDF document designated page\n"
    "output for lond pictures, easy to share\n","r");
    fprintf(stderr, "Reading Input:\n", "r");
    s=(char*)calloc(stderr, 500);
    free(s);
    return 0;
}