#include <stdio.h>
#include "large.h"
int main()
{
    int a = 10;
    char larg;
    large(a);
    if (a < 20)
    {
        l = large(number);
        printf("%d is less than 20\n", a);
        printf("The value of a is:%d", a) return 0;
    }